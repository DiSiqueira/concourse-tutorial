---
resources:
  - name: super-app
    type: git
    source:
      branch: ci
      uri: https://github.com/DiSiqueira/concourse-tutorial.git

jobs:
  - name: Test
    plan:
    - get: super-app
      trigger: true
    - task: Testing
      config:
        platform: linux
        image_resource:
          type: docker-image
          source: {repository: golang}
        run:
          path: ./test_fmt.sh
          dir: super-app/scripts

        inputs:
          - name: super-app
